// Vike's dynamic-segment syntax is `@param` inside a Route String, not the
// `[id]` bracket convention (that's Next.js/Remix) — the folder name here is
// just for human readability; this file is what actually makes it a param.
export default '/admin/leads/@id'
